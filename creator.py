import os
import sys

if len(sys.argv) != 2:
    print 'Missing param: number of accounts'
    exit(1)

NUM_ACCOUNTS = sys.argv[1]

created = 0 # number of accounts to be added to DB
while created < NUM_ACCOUNTS:
    accountName = ''
    accountName +=
    accountName +=
    accountName +=

    captchaText = os.popen('OCR.java')
